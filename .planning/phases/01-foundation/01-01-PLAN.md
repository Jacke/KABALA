---
phase: 01-foundation
plan: 01
type: execute
depends_on: []
files_modified: [package.json, tsconfig.json, tailwind.config.ts, next.config.ts, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css, .gitignore]
---

<objective>
Initialize Next.js project with TypeScript, Tailwind CSS, and basic project configuration.

Purpose: Establish the foundation that all features will build upon.
Output: Working Next.js app with TypeScript and Tailwind CSS configured.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js project with TypeScript and Tailwind</name>
  <files>package.json, tsconfig.json, tailwind.config.ts, next.config.ts, postcss.config.mjs, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css</files>
  <action>
    Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --use-npm` to initialize Next.js in current directory.

    This creates:
    - Next.js 14+ with App Router
    - TypeScript configuration
    - Tailwind CSS with PostCSS
    - ESLint configuration
    - src/ directory structure
    - @ import alias

    Accept defaults for all prompts (--yes flag or answer yes).

    Note: Use npm (not yarn/pnpm) for consistency.
  </action>
  <verify>
    npm run dev starts without errors.
    Visit http://localhost:3000 - default Next.js page loads.
    npm run build completes without errors.
  </verify>
  <done>Next.js dev server runs, build succeeds, TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Configure project structure and clean boilerplate</name>
  <files>src/app/page.tsx, src/app/globals.css, src/lib/.gitkeep, src/components/.gitkeep, src/types/.gitkeep, src/data/.gitkeep</files>
  <action>
    1. Clean src/app/page.tsx - replace with simple placeholder:
       ```tsx
       export default function Home() {
         return (
           <main className="min-h-screen p-8">
             <h1 className="text-4xl font-bold">KABALA</h1>
             <p className="mt-4 text-lg text-gray-600">Global Purchasing Power Dashboard</p>
           </main>
         );
       }
       ```

    2. Clean src/app/globals.css - keep only Tailwind imports:
       ```css
       @tailwind base;
       @tailwind components;
       @tailwind utilities;
       ```

    3. Create directory structure:
       - src/lib/ (utilities, helpers)
       - src/components/ (React components)
       - src/types/ (TypeScript types)
       - src/data/ (JSON data files)

       Add .gitkeep files to empty directories.

    4. Update .gitignore to include:
       - .env.local
       - .vercel
       - *.log
  </action>
  <verify>
    npm run dev shows clean KABALA homepage.
    npm run build succeeds.
    All directories exist: src/lib, src/components, src/types, src/data.
  </verify>
  <done>Clean project structure with KABALA branding, organized directories, build passes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run dev` starts successfully
- [ ] `npm run build` completes without errors
- [ ] Homepage shows "KABALA" title
- [ ] Directory structure: src/lib, src/components, src/types, src/data exist
- [ ] TypeScript compiles without errors
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No TypeScript errors
- Clean, organized project structure ready for development
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
